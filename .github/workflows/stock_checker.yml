name: Labubu Stock Checker

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes UTC
  workflow_dispatch:

jobs:
  check_stock:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Restore check count cache
        id: restore-cache
        uses: actions/cache@v3
        with:
          path: check_count.txt
          key: check-count-latest
          restore-keys: |
            check-count-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Labubu stock checker
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python labubu_checker.py

      - name: Save updated check count to new cache
        uses: actions/cache@v3
        with:
          path: check_count.txt
          key: check-count-${{ github.run_id }}

      - name: Echo updated cache key (debug info)
        run: echo "✅ Updated cache key: check-count-${{ github.run_id }}"
